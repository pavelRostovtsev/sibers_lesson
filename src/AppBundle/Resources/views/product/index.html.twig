{% extends '@App/layout.html.twig' %}

{% block title %}
    Products
{% endblock %}

{% block body %}

    <div class="alert alert-primary">
        {% for category in categories %}
            <a href="{{ path('product_by_category',{id: product.getId()}) }}">{{ product.getTitle }}</a> <br>
        {% endfor %}
    </div>

{% endblock %}

{% block stylesheets %}

{% endblock %}