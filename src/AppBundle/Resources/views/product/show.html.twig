{% extends '@App/layout.html.twig' %}

{% block title %}
    Product
{% endblock %}

{% block body %}
    <h1>{{ product.getTitle() }}</h1>
    <h3>{{ product.getDescription() }}</h3>
    <h3>{{ product.getPrice() }}</h3>
    <h2>{{ product.category.getName() }}</h2>
{% endblock %}

{% block stylesheets %}

{% endblock %}