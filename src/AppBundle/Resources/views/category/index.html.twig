{% extends '@App/layout.html.twig' %}

{% block title %}
    category
{% endblock %}

{% block body %}

    <div class="alert alert-primary">
        {% for category in categories %}
            <a href="{{ path('product_by_category', {id: category.id}) }}"><h2>{{ category.getName() }}</h2></a>
        {% endfor %}
    </div>

{% endblock %}

{% block stylesheets %}

{% endblock %}